# TODO: Modifier le header pour qu'il ne prenne pas toute la largeur, soit centré et légèrement plus bas

## Étapes à suivre :

  - Changer `width: 100%` à `width: 90%` pour réduire la largeur.
- [x] Modifier `src/Header/header.css` :
  - Changer `width: 100%` à `width: 90%` pour réduire la largeur.
  - Ajouter `left: 50%` et `transform: translateX(-50%)` pour centrer horizontalement.
  - Ajouter `top: 20px` pour positionner légèrement plus bas.
- [x] Tester le header sur différentes tailles d'écran pour s'assurer qu'il est centré et pas trop large.
- [ ] Ajuster si nécessaire la largeur ou la valeur top selon les retours visuels.
